# Prometheus: coleta por scrape as métricas que a API expõe em /metrics
# (instrumentação com prometheus_client, ver src/app/main.py).
global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: triagem-api
    static_configs:
      - targets: ['api:8000']
